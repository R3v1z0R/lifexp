import { PrimaryButton } from "@lifexp/web";

// The gold call-to-action. Names the outcome and keeps that word through the flow.

export const LogIt = () => <PrimaryButton>Log it</PrimaryButton>;

export const HostEvent = () => <PrimaryButton>Host event</PrimaryButton>;

export const Disabled = () => <PrimaryButton disabled>Not enough credits</PrimaryButton>;
